repos:
  - repo: local
    hooks:
      - id: zukey-sensitive-data
        name: ZUKEY sensitive-data scan
        entry: python scripts/check-sensitive-data.py --staged
        language: system
        pass_filenames: false
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
